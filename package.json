{
  "name": "stars-reborn",
  "version": "0.1.0",
  "private": true,
  "description": "Нативная Android-обёртка (Capacitor) над браузерной игрой. Живёт только в ветке capacitor, в main не мёржится — см. dev/docs/tasks/P-02.",
  "scripts": {
    "build": "node scripts/build-www.js",
    "sync": "npm run build && cap sync android",
    "apk": "npm run sync && cd android && .\\gradlew.bat assembleDebug",
    "install-apk": "\"%ANDROID_HOME%\\platform-tools\\adb.exe\" install -r android/app/build/outputs/apk/debug/app-debug.apk"
  },
  "dependencies": {
    "@capacitor/android": "8.5.0",
    "@capacitor/core": "8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.0"
  }
}
