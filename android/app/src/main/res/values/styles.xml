<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <!-- P-02: системные полосы под небо. На Android 13 (Redmi) окно ещё не
             edge-to-edge, и полосы рисуются своим цветом: статус-бар брал серый
             colorPrimaryDark из шаблона Capacitor, навигация — белый. Обе торчали
             из ночного неба. Красим в чёрный, значки — светлые.
             На SDK 35+ окно становится edge-to-edge, полосы прозрачны, и тогда
             работают безопасные зоны из css/style.css. -->
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightNavigationBar">false</item>
        <!-- P-02: небо доходит до самого верхнего края, включая полосу с вырезом
             под камеру. Без shortEdges там осталась бы чёрная планка вместо звёзд.
             Отступ под вырез при этом приезжает в CSS через env(safe-area-inset-top). -->
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>


    <!-- P-02: дефолтный splash Capacitor — светлая картинка с его логотипом,
         и при запуске ночной игры она бьёт по глазам вспышкой. Меняем на ровный
         чёрный: экран запуска совпадает с первым кадром игры, перехода не видно. -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@android:color/black</item>
        <item name="windowSplashScreenBackground">@android:color/black</item>
        <!-- P-02: параметры окна читаются при его создании, то есть из темы
             запуска. Без дубля здесь первый кадр приезжает с чужими цветами
             и без выреза, и запуск заметно моргает. -->
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightNavigationBar">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>